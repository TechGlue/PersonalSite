// declaring an interface for the NavBarRoutes
export interface NavBarRoutes {
  routeName: string;
  routePath: string; // leaving route path incase I need to use it for routing in the future
  fragment: string;
}
